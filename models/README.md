# Models Directory

This directory contains saved statistical models and model outputs.

## Guidelines

- Save models as `.RDS` or `.rda` files
- Include model metadata and descriptions
- Document model parameters and performance metrics
- Keep model training code in the scripts directory
